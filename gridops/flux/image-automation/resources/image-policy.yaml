apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: gridops
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: gridops

  filterTags:
    pattern: '^(?P<ts>[0-9]{8}-[0-9]{6})-[a-f0-9]{8}$'
    extract: '$ts'

  policy:
    alphabetical:
      order: asc